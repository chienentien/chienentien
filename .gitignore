<!DOCTYPE html>
<html lang="nl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Creatieve Studio</title>
  <style>
    :root {
      --lichtbeige: #ddd0bd;
      --groenlicht: #a4ad7c;
      --groendiep: #16421a;
      --taupe: #4a403a;
      --wit: #ffffff;
      --accent: #d18044;
    }

    body {
      margin: 0;
      font-family: 'Segoe UI', sans-serif;
      background-color: var(--lichtbeige);
      color: var(--taupe);
      line-height: 1.6;
    }

    header {
      background-color: var(--groendiep);
      color: var(--wit);
      padding: 4rem 2rem;
      text-align: center;
    }

    header h1 {
      font-size: 2.5rem;
      margin-bottom: 1rem;
    }

    header p {
      font-size: 1.2rem;
      margin-bottom: 2rem;
    }

    .button {
      background-color: var(--accent);
      color: var(--wit);
      padding: 12px 24px;
      border: none;
      border-radius: 30px;
      text-decoration: none;
      font-weight: bold;
    }

    section {
      padding: 4rem 2rem;
      max-width: 1000px;
      margin: 0 auto;
    }

    h2 {
      text-align: center;
      margin-bottom: 2rem;
      color: var(--groendiep);
    }

    .abonnementen {
      display: flex;
      gap: 2rem;
      flex-wrap: wrap;
      justify-content: center;
    }

    .plan {
      background-color: var(--wit);
      padding: 2rem;
      border-radius: 15px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.1);
      width: 250px;
      text-align: center;
    }

    .statistieken {
      display: flex;
      justify-content: space-around;
      flex-wrap

